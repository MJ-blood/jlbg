# 项目协作规范

## 适用范围与当前阶段

- 项目真实路径：`D:\data\project\jlbg`，属于 Windows 本地项目。
- 先遵守 `C:\Users\Admin\.codex\AGENTS.md`，再遵守本文件；回复以「mater」称呼用户，默认中文。
- 用户于 2026-09-08 明确确认 v0.2：「可以，采用这一版，开始开发吧」。已授权按首关详细设计与开发计划实施 P1–P6，包括项目级代码、构建配置、验证和离线包准备。
- 已批准范围内持续实现和验证，不重复请求开工确认。Mac 真机、签名账号等当前不可独立完成的事项保留待验证或待授权，先完成可独立推进的工作。
- 授权范围和新增决定统一记录到 `docs/development-plan.md`，不把文档完善误记成已开始开发。
- 最终目标是 Windows 和 macOS 本地浏览器离线游玩。当前 Windows 开发成功不能替代 Mac 真机和玩家离线包验收。
- 用户最新调整：继续推进玩法，当前暂不处理 macOS 版本、签名或玩家包。双平台最终目标保留，平台工作不再阻塞现阶段玩法迭代，也不重复追问 Mac 信息。
- 用户进一步明确：本轮不改界面，主要以《纪念碑谷》的空间玩法延伸第二、第三关，图片作为辅助参考。界面重做、存档、撤销等不列入当前关卡开发重点。
- 用户已确认三关方案：「可以，就按照你的想法进行开发吧」。已授权第二关升降搭乘、第三关压板与转桥组合、必要换关入口和对应回归验证，按 `docs/gameplay-expansion.md` 与开发计划 G1–G5 持续实施，不重复请求开工。

## 阅读顺序与文档职责

1. `README.md`：项目入口、当前阶段和阅读导航。
2. `docs/project-background.md`：需求来源、已确认事实、环境证据和边界。
3. `docs/development-proposal.md`：已批准的 v0.2 技术基线、范围与最终平台要求；当前优先级以开发计划为准。
4. `docs/development-plan.md`：阶段、验收标准、状态和后续续接点。
5. `docs/level-01-design.md`：首关具体流程、节点、道路、机关状态和验收用例；首关细节以此为准。
6. `docs/gameplay-expansion.md`：v0.3 三关递进、第二/三关规则与验收提案；与 v0.2 已实现内容分开标记。
7. `docs/validation-v0.3.md`：三关实际验证、构建版本、浏览器矩阵及未覆盖项；后续改动按受影响范围复验。

事实、建议、假设分开记录，不把已有讨论中的建议写成用户决定。仅在项目事实、验证结果或续接点变化时更新状态。

## 目录与命名

- 根目录保留项目规范、说明和包管理文件。
- `docs/` 存放前期背景、方案和计划；文件名使用英文小写与连字符，正文使用中文。
- `src/`、`tests/`、`scripts/`、`dist/` 按实施需要创建；没有静态素材时不创建空的 `public/`。验证产物仍写系统临时目录。
- 拟采用 `src/` 放代码、`public/` 放本地素材、`dist/` 放构建产物。第三方依赖由 npm 管理在 `node_modules/`。
- 素材命名表达用途，不使用 `final2`、`new-new` 等含糊后缀。代码命名遵循方案确认后的统一约定。

## 修改与清理

- 最小改动，保护用户已安装的依赖和已有文件，不顺手调整版本、格式或工具链。
- 需要调整项目规则时先改文档，再改实践。超过已批准范围的大改动须再次说明并确认。
- 删除文件或目录、可能丢失改动的 Git 操作、全局依赖或工具链修改、敏感配置修改、数据库变更和生产发布，遵守全局红线并先征求用户同意。
- 临时验证产物写入系统临时目录；不要在根目录积累截图、日志或实验文件。清理涉及删除时仍按上述规则处理。
- 不将密钥、凭据、个人信息写入代码、素材或日志；首版运行资源以本地文件为准。

## 验证与交付

- 文档阶段：核对引用路径、事实与建议的标记、方案和计划的一致性，以及是否误改代码或包管理文件。
- 开发阶段：执行与当前阶段对应的构建、交互、视错觉与离线检查；具体标准见开发计划。
- 依赖加载成功不等于浏览器渲染成功；构建成功不等于游戏可通关；离线承诺必须经过实际断网运行验证。
- 实现后未验证标记「待验证」；事实无法确认标记「待确认」；完成实现且通过相应验证才标记「已完成」。
- 交付说明包含具体变化、验证结果、限制和下一步。未实现功能或尚未具备的启动命令不得写成可用。
