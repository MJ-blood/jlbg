# 第一关视觉精修样板验证

日期：2026-09-09。工作区：`D:\data\project\jlbg`。本轮用户批准先精修一个人物与第一关，效果确认后再考虑其余关卡。

## 实现范围

- 第一关旅人采用独立的 `src/traveller.js`：曲面衣袍、开口披肩、帽檐与帽带、面部、胸针、手脚。增加迈步、摆臂、缓转、轻微待机呼吸，以及柔和脚影。
- 角色保持宽约 0.354、高约 0.822 的紧凑尺寸；结束时所有子部件和脚影统一淡出。开启系统「减少动态效果」时，取消装饰性步态、呼吸和转身缓动，仍保留必要的道路移动。
- 第一关建筑增加适度倒角、立体拱窗框、窗内横竖分隔、分层台座、贴合锥形柱身的细柱饰、桥底横梁、手轮轮辐和黄铜出口门框。手轮轮辐随手轮旋转。
- 首关采用较柔和的砂岩色，降低环境光、调整主光比例；脚影与建筑原有局部阴影共同维持层次。不启用会暴露错觉道路真实高度差的全场实时投影。
- 道路节点、机关状态、通行面尺寸和遮挡柱主体位置保持原值。其他五关沿用原有模型与灯光强度，UI 布局不变；无新依赖，无 Blender、外部素材或运行时外网依赖。

## 命令与构建

- `npm.cmd test`：44/44 通过，覆盖六关现有逻辑与可达状态恢复。
- `npm.cmd run build`：通过。最后两处模型修正为柱饰贴合与手轮轮辐随动；随后重建并完成下列最终 Edge / DPR 2 与构建验证。
- 最终 JS：`assets/index-BssCq7b_.js`，614.70 kB，gzip 162.59 kB。SHA-256：`E1B4308BE4C9660A66432AB3CBFE42283561037D1113F545F833732CB3230876`。
- CSS 沿用 `assets/index-DrBf9kjU.css`，5.61 kB。Vite 单块超过 500 kB 提示仍保留。
- 包配置和锁定文件无修改，构建继续保留历史散列文件；本次并未制作玩家包、推送或部署。

## 浏览器交互与场景

使用现有 Playwright 驱动本机 Chrome / Edge，鼠标点击道路与 DOM 按钮；开发模式只读诊断负责观察状态、投影与拾取，不修改游戏状态。交互验证使用模拟时钟推进，不能解释为真实玩家耗时。

| 浏览器 | 视口 / DPR | 结果 |
| --- | --- | --- |
| Chrome 152.0.7977.77 | 1440×900 / 1 | 第一关通关、双向接缝、重玩、行走中重置通过；此轮早于最后两处模型细节修正 |
| Edge 152.0.4191.66 | 1280×720 / 1 | 最终场景上述流程通过 |
| Chrome 152.0.7977.77 | 1440×900 / 2 | 最终场景上述流程通过 |

- 实际操作顺序：开始 → W → 转桥 → H → 转桥 → F → H → F → E。可三次跨越接缝并完成通关，反向道路正常；重玩回到开场，行走中重置回到 S。
- 接缝 R2/Q 投影差均为 0 CSS px；逐帧切换前后截图核对，增高后的帽子、身体与柔和脚影均被原遮挡柱隐藏。窗口、柱饰与桥底结构没有阻断上述道路拾取。
- 三组矩阵均切换进入其余五关并开始，确认场景渲染、idle 状态与无异常；此项为共享渲染循环抽检，不记为本轮其他五关完整通关。
- 每组进行八轮第二关/第一关切换，比较第二与第八轮，首关几何数均 145、纹理数均 3；无持续增长。首关约 147 个绘制调用、28,816 个三角形。
- 独立人物实例检查：减少动态效果时上下起伏为 0、转向立即到位；结束淡出 18 个网格的不透明度全部为 0；19 个几何、6 个材质、1 个纹理均收到释放事件。
- 所有上述浏览器 pageerror / console error 均为 0。1440×900 与 1280×720 实际截图检查模型与底部控件没有重叠。

## 构建资源隔离与短时帧采样

最终 dist 在 Chrome 和 Edge 的全新 1440×900 上下文中分别通关、反向过桥与重玩。请求路由禁用 HTTP 缓存、屏蔽 Service Worker，仅允许 `http://127.0.0.1:4174/`；仅请求本机 HTML、上述 JS/CSS，外网请求 0，脚本/控制台错误 0。正式构建不提供 `window.__courtyard`，使用已验证的鼠标坐标与页面按钮回放。

另外用未安装模拟时钟的新页面，各采样 120 个真实 requestAnimationFrame 间隔：Chrome 平均 15.99 ms、P95 17 ms；Edge 平均 16.02 ms、P95 18 ms。仅为本机无头浏览器开场短时采样，不代表其他设备或移动中、长时间游玩的性能。

## 复核材料与未覆盖范围

临时目录 `C:\Users\Admin\AppData\Local\Temp` 下 `jlbg-refine-*` 保存前后对比、接缝截图、人物放大检查图及 JSON。脚本为 `jlbg-refine-check.cjs`、`jlbg-refine-offline.cjs`、`jlbg-refine-avatar.cjs`，使用当前机器已有的 Playwright 和 Chrome / Edge，没有写入项目依赖。

尚待用户确认精修风格与实际观感；尚未推广到其余五关。未验证 Mac / Safari、其他显卡、系统实际断网、免 Node 玩家包。当前版本是已验证的首关样板，不等于最终双平台交付。
