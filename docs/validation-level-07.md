# 第七关原型验证

日期：2026-09-09。工作区：`D:\data\project\jlbg`。状态：第七关可玩原型已实现并通过下列工程验证；趣味性、正式美术深化与后五关未标记完成。

## 实现范围

- 新增「换景之庭」：S/H/K 三处观景台、两个离散观察角、三对条件连接和门前可返回的 G 回廊。最少换景三次通关。
- 镜头围绕固定中心转动；建筑和人物的世界位置不因换景改变。取景包围范围覆盖整段相机轨迹，不在旋转中反复缩放。
- 行走引擎支持多对独立错觉接缝，旧六关保留原有规则。过渡期间拒绝重复操作和移动，到位后提交新道路；减少动态效果使用 0.12 秒短切换。
- 建筑使用薄墙、悬挑肋梁、中空塔阶和铜色方向标；人物延续此前精修模型。章节列表实际提供七关，第八至十二关尚未实现。

## 逻辑与构建

`npm.cmd test`：**50/50 通过**，包含旧 44 项和第七关 6 项。新增覆盖标准解与必要换景、转换中位置保持/输入锁定、三对接缝往返、错误换景恢复、门前禁止换景、中途重置、减少动态效果、有效/无效角度投影，以及全部 **8 个稳定可达状态**的可完成性。最少换景次数为 **3**。

`npm.cmd run build`：成功。

- 入口 `dist/index.html`。
- JavaScript `dist/assets/index-DiOnTokA.js`：621.66 kB，gzip 165.21 kB。
- SHA256：`962102C1C043DFA28B93AE5984095CADD07CD20260E1F6DD4463E4969505858A`。
- CSS `dist/assets/index-DrBf9kjU.css`：5.61 kB。
- Vite 仍提示 JS chunk 大于 500 kB。本轮没有提高警告阈值或调整构建配置；旧哈希产物因保留构建策略仍在 dist，入口引用以上版本。

## 浏览器实测

测试通过真实页面按钮和鼠标射线点击操作。开发版诊断接口仅用于读取节点位置、状态及拾取；构建版不含该接口，重放实际鼠标坐标。

| 环境 | 验证结果 |
| --- | --- |
| Windows Chrome 152.0.7977.77，1440×900，DPR 1 | 第七关通关、三对接缝双向返回、途中帧截图、镜头中途重置、重玩通过 |
| Windows Edge 152.0.4191.66，1280×720，DPR 1 | 同上通过 |
| Windows Chrome，1440×900，DPR 2 | 第七关完整通关、返回、接缝与重置通过 |
| Chrome 额外 900×760 | 两个视角中稳定目标均在画布内；画布与顶部、底部控件分离 |
| 减少动态效果 | 上述第七关浏览器流程末尾验证短切换，200 ms 内已回到 idle/B |
| 旋转中切换章节 | 第七关旋转中切至第一关，再回第七关：入口 A 视角与 intro 正常 |
| 旧六关回归 | Chrome / Edge 均以原来的鼠标与按钮流程完成六关，包含原脚本中的回程、搭乘与重玩 |

三对接缝在对应视角下投影误差小于 1 CSS px，非对应视角下间距大于 20 CSS px。检查转换瞬间前后帧：人物与脚影均由实体墙遮挡，不把二维对齐本身当作遮挡验证。

实画面阶段修正两处问题：塔前横梁移至路面下方，避免遮住稳定停留的角色；J/L 和 N/O 遮挡墙放到该对端点中更靠近镜头的一侧，修正「投影相同但人物在墙前突然出现」的深度错误。前三关和旧机关几何未因这些修正改变。

切换第一/七关四轮后，第七关保持 104 geometry、2 texture；静止帧 106 draw calls、18,812 triangles，没有逐轮增长。Chrome 第七关静止场景独立采样 120 帧，帧间隔中位数 16.7 ms、P95 16.8 ms。仅代表本机短时采样，不代表低配置设备或长期表现。

## 构建资源外网隔离

Chrome/Edge 新建浏览器上下文，禁用缓存、阻止 Service Worker，路由仅允许 `http://127.0.0.1:4174` 资源，所有外网请求均阻止。两者均完成旧六关及第七关通关、返回和重玩；控制台/页面错误为 0，外网资源请求为 0。第七关冷加载仅请求本地 HTML、JS、CSS。

这验证了构建资源独立性，没有关闭系统网络适配器；仍由本机 Vite preview 提供文件，不是免 Node 玩家包验证。Windows 实测不替代 Mac/Safari 真机验证，后者按用户指示继续暂缓。

## 证据与下一阶段

临时验证产物位于系统临时目录 `C:\Users\Admin\AppData\Local\Temp`：

- `jlbg-seven.cjs`、`jlbg-seven-{chrome,edge,chrome-dpr2,chrome-offline,edge-offline}.json` 与对应状态/接缝截图。
- `jlbg-seven-layout.cjs`、`jlbg-seven-layout.json`：视口、帧采样、旋转中换关。
- `jlbg-six-art.cjs` 与本轮重新生成的 Chrome/Edge 开发版、外网隔离结果：前六关回归。

下一步是第七关实际试玩：观察玩家是否理解断口连接、是否能在塔前发现上层路线、是否因换景与遮挡迷失。此结果将用于原型调整，再进入第八关镜门原型。没有将程序通关作为「已验证好玩」的依据。本轮未提交、推送或部署。
